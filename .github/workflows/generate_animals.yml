name: Generate Animals JSON

on:
  push:
    paths:
      - 'generator/**'
  workflow_dispatch:
    inputs:
      force_update:
        description: 'Force re-fetch all data'
        required: false
        default: 'true'

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Clean up ALL old data directories
        run: |
          echo "🧹 Cleaning up old data directories..."
          rm -rf generator/data/
          rm -rf data/
          echo "✅ Old directories removed"

      - name: Run generator (ALWAYS FORCE)
        env:
          API_NINJAS_KEY: ${{ secrets.API_NINJAS_KEY }}
        run: |
          cd generator
          python generate_animals.py --force

      - name: Debug - Show what was created
        run: |
          echo "=== Repo Root ==="
          ls -la
          echo "=== data/ directory ==="
          ls -la data/ 2>/dev/null || echo "data/ not found"
          echo "=== data/animal_stats/ file count ==="
          ls data/animal_stats/ 2>/dev/null | wc -l || echo "0"
          echo "=== generator/data/ (should not exist) ==="
          ls generator/data/ 2>/dev/null && echo "ERROR: Old directory still exists!" || echo "✅ generator/data/ does not exist (good!)"

      - name: Pull latest changes before push
        env:
          MY_PAT: ${{ secrets.MY_PAT }}
        run: |
          git config --local user.name "GitHub Actions"
          git config --local user.email "actions@github.com"
          
          echo "🔄 Pulling latest changes..."
          git pull --rebase https://$MY_PAT@github.com/${{ github.repository }} main || echo "No remote changes to pull"
          
          echo "📊 Git status:"
          git status
          
          echo "📁 Adding files..."
          git add data/animals.json
          git add data/animal_stats/
          
          echo "💬 Committing..."
          git commit -m "🤖 Auto-update animal data [skip ci]" || echo "No changes to commit"
          
          echo "🚀 Pushing..."
          git push https://$MY_PAT@github.com/${{ github.repository }} HEAD:main
